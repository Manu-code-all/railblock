# Python
__pycache__/
*.pyc
.pytest_cache/

# Local runtime data — regenerate with `python -m railblock.seed`
prototype/railblock.db
prototype/st.log

# Local tool/session config, not project source
prototype/.agents/
prototype/.claude/

# React frontend — reinstall with `npm install` in prototype/web
prototype/web/node_modules/
prototype/web/dist/
prototype/web/.vite/

# Large raw video footage and borrowed reference clips — not part of
# the demo deliverable. The finished MVP video lives in video/final/.
video/raw/
video/edit/
reference/

# OS cruft
.DS_Store
Thumbs.db
